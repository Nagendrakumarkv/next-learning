import { Metadata } from "next";

export const metadata = {
  title: "Contact - Next.js",
  description: "Contact page SEO",
} as Metadata;

export default function Settings() {
  return <h1>Settings Page</h1>;
}
